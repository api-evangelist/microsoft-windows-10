naftiko: 1.0.0-alpha2
info:
  label: Microsoft Windows 10 Windows Notifications API — Badge Notifications
  description: 'Microsoft Windows 10 Windows Notifications API — Badge Notifications. 1 operations. Lead operation: Microsoft
    Windows 10 Update a badge notification. Self-contained Naftiko capability covering one Microsoft Windows 10 business surface.'
  tags:
  - Microsoft Windows 10
  - Badge Notifications
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_WINDOWS_10_API_KEY: MICROSOFT_WINDOWS_10_API_KEY
capability:
  consumes:
  - type: http
    namespace: notifications-badge-notifications
    baseUri: https://api.windows.com
    description: Microsoft Windows 10 Windows Notifications API — Badge Notifications business capability. Self-contained,
      no shared references.
    resources:
    - name: notifications-badge
      path: /notifications/badge
      operations:
      - name: updatebadgenotification
        method: POST
        description: Microsoft Windows 10 Update a badge notification
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: notifications-badge-notifications-rest
    port: 8080
    description: REST adapter for Microsoft Windows 10 Windows Notifications API — Badge Notifications. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/notifications/badge
      name: notifications-badge
      description: REST surface for notifications-badge.
      operations:
      - method: POST
        name: updatebadgenotification
        description: Microsoft Windows 10 Update a badge notification
        call: notifications-badge-notifications.updatebadgenotification
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: notifications-badge-notifications-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Windows 10 Windows Notifications API — Badge Notifications. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: microsoft-windows-10-update-badge
      description: Microsoft Windows 10 Update a badge notification
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: notifications-badge-notifications.updatebadgenotification
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
