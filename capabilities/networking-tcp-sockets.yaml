naftiko: 1.0.0-alpha2
info:
  label: Microsoft Windows 10 Windows Networking API — TCP Sockets
  description: 'Microsoft Windows 10 Windows Networking API — TCP Sockets. 1 operations. Lead operation: Microsoft Windows
    10 Create a TCP stream socket. Self-contained Naftiko capability covering one Microsoft Windows 10 business surface.'
  tags:
  - Microsoft Windows 10
  - TCP Sockets
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_WINDOWS_10_API_KEY: MICROSOFT_WINDOWS_10_API_KEY
capability:
  consumes:
  - type: http
    namespace: networking-tcp-sockets
    baseUri: https://api.windows.com
    description: Microsoft Windows 10 Windows Networking API — TCP Sockets business capability. Self-contained, no shared
      references.
    resources:
    - name: networking-sockets-tcp
      path: /networking/sockets/tcp
      operations:
      - name: createtcpsocket
        method: POST
        description: Microsoft Windows 10 Create a TCP stream socket
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: networking-tcp-sockets-rest
    port: 8080
    description: REST adapter for Microsoft Windows 10 Windows Networking API — TCP Sockets. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/networking/sockets/tcp
      name: networking-sockets-tcp
      description: REST surface for networking-sockets-tcp.
      operations:
      - method: POST
        name: createtcpsocket
        description: Microsoft Windows 10 Create a TCP stream socket
        call: networking-tcp-sockets.createtcpsocket
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: networking-tcp-sockets-mcp
    port: 9090
    transport: http
    description: MCP adapter for Microsoft Windows 10 Windows Networking API — TCP Sockets. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: microsoft-windows-10-create-tcp
      description: Microsoft Windows 10 Create a TCP stream socket
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: networking-tcp-sockets.createtcpsocket
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
